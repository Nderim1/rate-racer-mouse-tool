---
interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} | TestMyRig Blog</title>
    <meta name="description" content={description || "Latest posts from TestMyRig - insights, guides, and news about gaming peripherals and testing"} />
    
    <!-- Open Graph -->
    <meta property="og:title" content={`${title} | TestMyRig Blog`} />
    <meta property="og:description" content={description || "Latest posts from TestMyRig - insights, guides, and news about gaming peripherals and testing"} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={`https://testmyrig.com/blog${Astro.url.pathname === '/blog' ? '' : Astro.url.pathname.replace('/blog', '')}`} />
    <meta property="og:site_name" content="TestMyRig Blog" />
    
    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={`${title} | TestMyRig Blog`} />
    <meta name="twitter:description" content={description || "Latest posts from TestMyRig - insights, guides, and news about gaming peripherals and testing"} />
    
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="canonical" href={`https://testmyrig.com/blog${Astro.url.pathname === '/blog' ? '' : Astro.url.pathname.replace('/blog', '')}`} />
    <style>
      :root {
        /* Light Retro Minimalist Theme - Matching Main App */
        --color-bg: #fdfbf9; /* hsl(40, 20%, 99%) */
        --color-text: #2a2422; /* hsl(20, 10%, 15%) */
        --color-text-muted: #7a726f; /* hsl(30, 5%, 45%) */
        --color-border: #dcd6d1; /* hsl(30, 10%, 85%) */
        --color-card-bg: #f7f5f2; /* hsl(40, 20%, 97%) */
        --color-primary: #f26619; /* hsl(24, 95%, 53%) */
      }

      @media (prefers-color-scheme: dark) {
        /* Optional: Keep a dark mode or force light mode for now as per request "Light, minimalistic theme" */
        /* For now, we will override dark mode to keep the requested light theme consistent, 
           or we could provide a dark retro alternative. 
           Given the prompt "Redesign... using a light, minimalistic theme", I will stick to the light theme 
           even in dark mode preference, or provide a very subtle dark version. 
           Let's stick to the light theme for consistency as requested. */
        :root {
           --color-bg: #fdfbf9;
           --color-text: #2a2422;
           --color-text-muted: #7a726f;
           --color-border: #dcd6d1;
           --color-card-bg: #f7f5f2;
           --color-primary: #f26619;
        }
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        background: var(--color-bg);
        color: var(--color-text);
        line-height: 1.6;
      }

      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 2rem 1.5rem;
      }

      header {
        border-bottom: 1px solid var(--color-border);
        padding-bottom: 2rem;
        margin-bottom: 3rem;
      }

      .site-title {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--color-primary);
        text-decoration: none;
        display: inline-block;
        margin-bottom: 0.5rem;
      }

      .site-title:hover {
        opacity: 0.8;
      }

      nav {
        display: flex;
        gap: 1.5rem;
        margin-top: 1rem;
      }

      nav a {
        color: var(--color-text-muted);
        text-decoration: none;
        font-size: 0.95rem;
        transition: color 0.2s;
      }

      nav a:hover {
        color: var(--color-primary);
      }

      main {
        min-height: 60vh;
      }

      footer {
        margin-top: 4rem;
        padding-top: 2rem;
        border-top: 1px solid var(--color-border);
        text-align: center;
        color: var(--color-text-muted);
        font-size: 0.9rem;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <a href="/blog/" class="site-title">TestMyRig Blog</a>
        <nav>
          <a href="/">← Back to TestMyRig</a>
          <a href="/blog/">All Posts</a>
        </nav>
      </header>
      <main>
        <slot />
      </main>
      <footer>
        <p>&copy; {new Date().getFullYear()} TestMyRig. All rights reserved.</p>
      </footer>
    </div>
  </body>
</html>
